# Comma-separated list of allowed site URLs / origins that can call the
# /image metadata function and load files under /assets/drawings/*
# (enforced by netlify/edge-functions/media-gate.js).
ALLOWED_ORIGINS=http://localhost:8888,http://localhost:3000,http://127.0.0.1:8888
