  - [ ] Can you build a dev/Editor UI for entering metadata for images in local dev mode? The entered information will be written to "C:\Users\kaywi\dev\CodedByKay.ArtShow\netlify\functions\data\artwork.json". I can't store the metadata in a database, but should we use a web DB to store the information as we edit it and then save it back to the JSON file (overwriting it if it exists)?